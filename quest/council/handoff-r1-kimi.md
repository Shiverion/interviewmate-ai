# Kimi K3 — handoff.md v1, round 1 (2026-09-21)


**1. Can §1 support a solo policy change?** Almost. The first thing I'd have to look up that §1 never states: the actual provider catalog and env-var names (`src/lib/ai/catalog.ts`). §1.2 says configuration is `env[PROVIDERS[i].env]?.trim()`, but no env var is named anywhere in §1 (only `OPENAI_API_KEY` appears, in a demo comment), and provider order matters for `substitute`. A close second: that `requested` is validated by `isProvider` per route.

**2. Fact-check.** Numbers reconcile: 703+7+25=735; 735+214=949; 949+191=1140; 25+2=27 suites; diff sizes (37/44/46) match decision-record §5 Y3 including the 4-line comment commit. All five policy literals match the route excerpts verbatim. §1.5 bullets 2–5 check out against the evidence (page.tsx:330-332 forces `allowFallback` true when `scheduled`; caller branch passes `callerKey` untrimmed while server keys are trimmed; `evaluate:62` and `demo:60` throw into `catch`; no route reads `substituted`). **Bullet 1 is itself wrong**, though: it claims the "requested provider or fail" reading holds "only on `scheduled`" — but the resolver's `caller` branch *never* substitutes, so on `evaluate`'s caller path with `allowFallback: false` the requested provider is also used as-is. The "false assumption" is true for that path.

**3. Exercise (§4).** Bounded and under an hour, footprint matches the test structure (resolver rows are per-path branches inside the fixture loop; harness cases are tagged per path). One hidden wrinkle: the resolver test *duplicates* each route's policy literal by hand, so "update the rows for that policy literal" really means finding a stale copy. **Bigger problem: §4.1's rationale is factually incorrect.** Flipping only `onUnconfigured` to `"proceed"` leaves `allowFallback: true`, so the hosted admin requesting Gemini on an OpenAI-only server does *not* "fail through `assessEvidence`" — the resolver returns `provider: gemini, key: undefined` with `fallbackKeys: {openai: …}`, `assessEvidence` skips the empty primary and the fallback rescues it. The evaluation still succeeds; only the attempt sequence and `substituted` change. §4.2 is a fair comparison (same semantic change, no safety net).

**4. Checklist (§2).** Concise, ordered, pass/fail; item 2's grep silently relies on `scheduled/` living under `api/evaluate/` (it does, so `-r` covers it — worth one word). Missing: an item that `substituted` isn't surfaced in any response (§1.5's last bullet, a live follow-up).

**5. Metrics (§3).** Measured vs estimated labelled correctly; nothing exceeds the decision record. Three different "before" commits (`50fa2dc`, `d09182e`, `e25d63e`) are each labelled but easy to conflate. The exercise-timing row is `pending` — and §4.4 is entirely pending, so the Quest's fallback ("demonstrate the handoff yourself and label that limitation") is **not yet satisfied** by the document as written.

CHANGE: (1) Correct §4.1's rationale — with `allowFallback` still true the request does not fail, it is rescued by fallback; restate the observable difference (primary attempt with no key, `substituted`, attempt sequence) or also require `allowFallback: false`. (2) Fix §1.5 bullet 1 — the caller path with `allowFallback: false` also uses the requested provider untouched, so "only on scheduled" overstates the correction. (3) Complete §4.4 with the self-performed, n=1 record (and the labelled limitation) before shipping, since a fully-pending table does not meet the Quest's demonstrate-it-yourself fallback.

