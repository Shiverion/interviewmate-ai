# Council on the handoff exercise (2026-09-22) — three advisors, one question

Question: can the self-performed timed run be skipped given `agent-notes.md` and `agents.md`; and how to treat a second opinion proposing (a) a final §4.4 shape with a limitation paragraph, (b) fresh-agent before/after runs under fairness rules, (c) renaming the metric row "Files to edit…" → "Change surface and verification coverage", (d) a finalize sequence. Brief: `briefs/` (exercise-brief); bundle held `handoff.md` §3–§4 and `intent.md` §9.

| Advisor | Skip the self-run? | (a) | (b) | (c) | (d) |
|---|---|---|---|---|---|
| Fresh Opus | No — the brief's Task 5 fallback requires a demonstration; the stopwatch is only our own promise; retract or perform, never leave pending | Adopt; add "after run performed second" | Supplement only, never substitute | Adopt ("the current name is literally false") | Adopt; fix the `e25d63e` header |
| Codex sol | No — same; if timing dropped, retract explicitly in three documents | Adopt; header must say 50fa2dc; name model/effort if AI-assisted | Supplement; a better-controlled probe of fresh-context discoverability but weaker evidence of "another engineer" | Adopt | Adopt; also fix intent.md's outdated exercise description |
| Kimi K3 | No — "retracting is worse than running" | Adopt, sharpened: the author wrote §1 and the after-state change had been performed once; disclose | Adopt fairness rules; reject as replacement | **Reject** (reopens an approved section for style) | Adopt, minus the rename |

Decision (Claude, adopted by Iqbal): perform the self-run (primary); run fresh-agent probes as a separately labelled supplement (Sonnet, identical task text, no build memory); adopt (a) with Kimi's sharpening; adopt (c) by majority and log it; fix the header; one factual consistency pass instead of a new review round. Iqbal's instruction: agent results must not enter the primary §4.4 table.
