# Codex gpt-6-astra — directive.md round 2 critique (2026-09-20)

Round-1 resolution: Codex point 1 is resolved for substitution and browser overrides (4.1–4.2, 5.2); point 2 is resolved for absent keys, empty hosted configuration, and lazy parsing (Parts 3–4); point 3 is partially resolved by the real-handler baseline (5.2–5.3), with coverage gaps below. Provider-attempt verification is partially resolved (5.4); accounting verification is resolved in design (4.2, 5.5). Documentation scope and scoped Y2 are resolved (Part 3, Part 7); reproducible yardsticks are partially resolved. Kimi's evaluate, demo refusal, lazy parsing, documentation exemption, and approval-gate points are resolved (Parts 2–4, 6). Baseline provenance is substantially resolved (5.3); redundant-section cuts are resolved.

Part 2's three evaluate rows match evaluate:34–72. Scheduled's selection/error references are correct; trimming and fallback actually appear at :61–68 and should be cited. Both demo selection references are correct, but the table omits an important distinction: demo always passes an object, including {} when fallback is disabled (:51–63). Also, "keys present" means truthy raw values (:54), including whitespace-only strings. Availability requires nonempty trimmed OPENAI_API_KEY (ledger:92), not merely a set variable. An unconfigured demo primary can still produce the deterministic no-evidence result (assess:23–29), so "then fails or falls back" is incomplete. Accounting references are correct.

The five policies preserve provider choice and remove the unintended refusals, but they do not reproduce every argument tuple. With demo/no-grant/allowFallback:false, 4.1 returns undefined fallbackKeys where today's route passes {}. This would make a preserved harness case fail at H. Specify route adaptation or extend the contract. Explicitly settle empty-string and whitespace-only demo credentials under fix-2 rather than silently equating trimming with omission.

Commit H makes the focused baseline reproducible; resolver tests referencing the not-yet-created module must remain outside that baseline invocation. The harness is feasible with isolated module mocks, a scheduled session.ref.update stub, valid visitor inputs, and availability mocks reproducing the actual gate. Add whitespace-only/empty credentials, malformed fallback headers with permission on/off, no-evidence cases, and error-body assertions. Part 5.4's attempt oracle is incorrect: assess:30–39 excludes the primary from fallbacks, uses PROVIDERS order rather than object insertion order, and skips missing credentials. Specify eligible evidence and scripted outcomes.

Intent §5/§8 and the required Quest elements are otherwise represented. However, Parts 3/7's literal line-only restriction forbids necessary imports and scheduled call-argument edits (:67–68). Expand permitted edit regions before enforcing that rubric.

CHANGE — 1. Correct demo argument preservation and table qualifications. 2. Repair the attempt oracle and baseline coverage. 3. Permit necessary wiring edits explicitly.
